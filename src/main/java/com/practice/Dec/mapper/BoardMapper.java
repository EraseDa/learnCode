package com.practice.Dec.mapper;import com.practice.Dec.domain.BoardVO;import com.practice.Dec.domain.Criteria;import org.apache.ibatis.annotations.Mapper;import org.apache.ibatis.annotations.Param;import org.apache.ibatis.annotations.Select;import java.util.List;@Mapperpublic interface BoardMapper {    //@Select("select * from chj_tbl_board where bno > 0") //해당 sql문은 mapper를 통해 작성, 연결했으므로 주석처리    public List<BoardVO> getList();    //13.1 MyBatis처리와 테스트    public List<BoardVO> getListWithPaging(Criteria cri);    //8.2.1 create(insert)처리    public void insert(BoardVO board);    public void insertSelectKey(BoardVO board);    //8.2.2 read(select)처리    public BoardVO read(Long bno);    //8.2.3 delete처리    public int delete(Long bno); //삭제된 데이터의 수를 반환할거니까 리턴타입을 int로    //8.2.4 update처리    public int update(BoardVO board); //몇 개의 데이터가 수정되었는지 int로 반환    //14.6 MyBatis에서 전체 데이터의 개수 처리    public int getTotalCount(Criteria cri);    //20.1.1 replyCnt 업데이트하는 메서드. 댓글 추가 삭제시 수를 반영하기 위해    public void updateReplyCnt(@Param("bno") Long bno, @Param("amount") int amount);}